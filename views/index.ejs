<!DOCTYPE html>
<html>
<head>
	<title><%= title %></title>
	<%- include('./includes/meta-includes'); %>
	<%- include('./includes/css-includes'); %>
	<link rel="stylesheet" type="text/css" href="css/style.css">
</head>
<body>
<div>
<%- include('./includes/header-includes'); %>
  <div class="ui main text container">
    <div class = "banner">Welcome to MCKVIE-CC</div>
    <div class="list">
		<div class="ui active centered inline loader"></div>
		<ul>
			<!-- Dummy data -->
			<li>
				<a href="">Name of User</a>
				<span>( Team Name )</span>
				<strong>Commits Count</strong>
			</li>
			<li>
				<a href="">Name of User</a>
				<span>( Team Name )</span>
				<strong>Commits Count</strong>
			</li>
			<li>
				<a href="">Name of User</a>
				<span>( Team Name )</span>
				<strong>Commits Count</strong>
			</li>
			<!-- Dummy data ends -->
		</ul>
	</div>
  </div>

	<%- include('./includes/js-includes'); %>
	<script type="text/javascript">
		$(document).ready(function(){
			$.get('/apis/user_commits', function(data){
				let listHtml = '<ul>'
				data.forEach(function(el){
					listHtml += `<li><a href="/profile/${el.id}">${el.name}</a><span>(${el.team_name})</span> <strong>${el.commit_count}</strong></li>`
				})
				listHtml += '</ul>'
				$('.list').html(listHtml)
			})
		});
	</script>
</div>
</body>
</html>